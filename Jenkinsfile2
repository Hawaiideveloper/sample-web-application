#!/usr/bin/env groovy

@Library('Jenkins-shared-library@master') _ //master or whatever branch

pipeline{

     
        
        stages{

              stage('maven build'){
                  steps{
                      script{
		    	                echo "mvn clean install"
                      	  }
               	     }  
                 }	
                 
                 stage ('Check logs') {
                    steps {
                        filterLogs ('WARNING', 1)
                    }
                }
		
           }	       	     	         
}
